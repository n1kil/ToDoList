<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="ToDoList" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <DataGrid x:Name="dgToDoApp" 
                  FontSize="16" 
                  FontWeight="Bold" 
                  Margin="10" 
                  AutoGenerateColumns="False" 
                  CanUserSortColumns="True" 
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Text}" 
                                    Header="Задача" 
                                    Width="*"
                                    SortMemberPath="Text"/>
                <DataGridTextColumn Binding="{Binding Path=Priority}" 
                                    Header="Приоритет" 
                                    Width="100"
                                    SortMemberPath="Priority"/>
                <DataGridTextColumn Binding="{Binding Path=Category}" 
                                    Header="Категория" 
                                    Width="100"
                                    SortMemberPath="Category"/>
                <DataGridTextColumn Binding="{Binding Path=Deadline}" 
                                    Header="Дедлайн" 
                                    Width="100"
                                    SortMemberPath="Deadline"/>
                <DataGridCheckBoxColumn Binding="{Binding Path=IsDone, UpdateSourceTrigger=PropertyChanged}" 
                                        Header="Выполнена ли задача?" 
                                        Width="190"
                                        SortMemberPath="IsDone"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
